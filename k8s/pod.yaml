apiVersion: v1
kind: Pod
metadata:
  name: "goserver"
  labels:
    app: "goserver"
spec:
  containers:
    - name: "goserver"
      image: "zaphodbrox/hello-go-k8s:latest"
      ports:
        - containerPort: 8080
